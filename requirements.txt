# JARVIS MK-X Requirements
# Direct dependencies of the active runtime (audit-verified 2026-08-08).
# Generated from an AST scan of the active tree; versions pinned to the venv.

# === Config ===
toml==0.10.2
PyYAML==6.0.3

# === LLM Providers ===
groq==1.5.0
google-genai>=1.0.0
openai==2.46.0
ollama==0.6.2

# === MCP ===
mcp==1.28.1

# === Transport / HTTP ===
httpx==0.28.1
orjson==3.11.9
websockets==16.1.1  # WebSocket transport for browser/terminal clients

# === System ===
psutil==7.2.2
numpy==2.4.6
filelock==3.32.0  # cross-process daemon single-instance lock

# === CLI / UI ===
typer==0.27.1
rich==15.0.0
textual==8.2.8

# === Optional (lazy, guarded imports) ===
# sentence-transformers==5.6.1   # memory embeddings (~2GB torch)
# aiosqlite>=0.19.0              # runtime.telemetry persistence
# sounddevice==0.5.5             # audio health check
# pynvml>=11.0.0                 # GPU monitoring (core/resource_manager)
