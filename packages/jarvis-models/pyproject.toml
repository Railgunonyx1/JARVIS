[project]
name = "jarvis-models"
version = "0.1.0"
description = "JARVIS model types, gateway seam, and provider contracts"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
