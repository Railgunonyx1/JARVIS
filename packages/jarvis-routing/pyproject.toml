[project]
name = "jarvis-routing"
version = "0.1.0"
description = "JARVIS intent router and provider routing contracts"
requires-python = ">=3.11"
dependencies = ["jarvis-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
