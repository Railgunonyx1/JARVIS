[project]
name = "jarvis-mcp"
version = "0.1.0"
description = "JARVIS MCP/ACP/Codex protocol seams"
requires-python = ">=3.11"
dependencies = ["jarvis-core", "jarvis-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
