[project]
name = "jarvis-core"
version = "0.1.0"
description = "JARVIS core: agent loop, state machine, harness, event bus, observability"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
