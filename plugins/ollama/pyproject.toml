[project]
name = "jarvis-ollama"
version = "0.1.0"
description = "JARVIS Ollama provider plugin"
requires-python = ">=3.11"
dependencies = ["jarvis-core", "jarvis-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
