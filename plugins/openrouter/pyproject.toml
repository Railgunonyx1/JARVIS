[project]
name = "jarvis-openrouter"
version = "0.1.0"
description = "JARVIS OpenRouter provider plugin"
requires-python = ">=3.11"
dependencies = ["jarvis-core", "jarvis-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
