[project]
name = "jarvis-groq"
version = "0.1.0"
description = "JARVIS Groq provider plugin"
requires-python = ">=3.11"
dependencies = ["jarvis-core", "jarvis-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
